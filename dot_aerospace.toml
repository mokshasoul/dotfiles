start-at-login = true
config-version = 2

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
  "exec-and-forget /opt/homebrew/bin/borders",
  # "exec-and-forget /opt/homebrew/bin/sketchybar"
]

# Notify Sketchybar about workspace change
exec-on-workspace-change = [
  # "/bin/bash",
  # "-c",
  # "sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE"
]

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 32

# Possible values: tiles|accordion
default-root-container-layout = "tiles"

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = "auto"

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ["move-mouse monitor-lazy-center"]

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app

automatically-unhide-macos-hidden-apps = true

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config version is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = ["1", "W", "2", "3", "4", "M", "T", "0"]

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = []

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = "qwerty"

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 12
inner.vertical = 12
outer.right = 12
outer.left = 12
outer.bottom = 8
outer.top = 8

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]
# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
#                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
#                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
#                   sectionSign (ISO keyboards only, european keyboards only)
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
alt-enter = "exec-and-forget open -n /Applications/Ghostty.app"
alt-g = "exec-and-forget open -a Finder.app"
cmd-h = []  # Disable "hide application"
cmd-alt-h = []  # Disable "hide others"

alt-ctrl-enter = "fullscreen --no-outer-gaps"
# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = "layout tiles horizontal vertical"
alt-comma = "layout accordion horizontal vertical"

# Smart resize
alt-shift-minus = "resize smart -50"
alt-shift-equal = "resize smart +50"

# See: https://nikitabobko.github.io/AeroSpace/commands#focus

alt-h = "focus --wrap-around left"
alt-j = "focus --wrap-around down"
alt-k = "focus --wrap-around up"
alt-l = "focus --wrap-around right"

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = "move --boundaries-action stop left"
alt-shift-j = "move --boundaries-action stop down"
alt-shift-k = "move --boundaries-action stop up"
alt-shift-l = "move --boundaries-action stop right"

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-0 = "workspace 0"
alt-1 = "workspace 1"
alt-2 = "workspace 2"
alt-3 = "workspace 3"
alt-4 = "workspace 4"
alt-m = "workspace M"
alt-t = "workspace T"  # T like trash
alt-w = "workspace W"

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-0 = "move-node-to-workspace 0"
alt-shift-1 = "move-node-to-workspace 1"
alt-shift-2 = "move-node-to-workspace 2"
alt-shift-3 = "move-node-to-workspace 3"
alt-shift-4 = "move-node-to-workspace 4"
alt-shift-m = "move-node-to-workspace M"
alt-shift-t = "move-node-to-workspace T"
alt-shift-w = "move-node-to-workspace W"

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-backtick = "exec-and-forget aerospace focus-back-and-forth || aerospace workspace-back-and-forth"
alt-tab = "workspace-back-and-forth"
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = "move-workspace-to-monitor --wrap-around next"

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-space = "mode leader"
alt-shift-space = "layout floating tiling"
alt-shift-c = "reload-config"

[mode.leader.binding]
enter = "mode main"
esc = "mode main"
backspace = ["flatten-workspace-tree", "mode main"]  # reset layout
q = ["close-all-windows-but-current", "mode main"]
r = ["mode resize"]
h = ["layout h_tiles", "mode main"]
v = ["layout v_tiles", "mode main"]
f = [
  "layout floating tiling",
  "mode main",
]  # Toggle between floating and tiling layout

[mode.resize.binding]
enter = "mode main"
esc = "mode main"
l = "resize width +50"
h = "resize width -50"
k = "resize height -50"
j = "resize height +50"
alt-shift-h = ["join-with left"]
alt-shift-j = ["join-with down"]
alt-shift-k = ["join-with up"]
alt-shift-l = ["join-with right"]

[workspace-to-monitor-force-assignment]
0 = "secondary"
1 = "main"  # Non-main monitor in case when there are only two monitors
2 = "main"  # Case insensitive regex substring
3 = "secondary"  # Non-main monitor in case when there are only two monitors
4 = "secondary"  # Case insensitive regex substring

# Workarounds

[[on-window-detected]]
check-further-callbacks = true
run = ["layout vertical", "layout tiling"]

[[on-window-detected]]
if.app-id = "com.1password.1password"
if.window-title-regex-substring = ".*(quick access|lock screen).*"
run = "layout vertical"

# Workspace
[[on-window-detected]]
if.app-id = "com.mitchellh.ghostty"
run = [
  "layout tiling",
  "layout tiles vertical",
  "move-node-to-workspace --focus-follows-window 1"
]

[[on-window-detected]]
if.app-id = "com.apple.Safari"
if.window-title-regex-substring = "personal"
run = ["layout tiling v_tiles", "move-node-to-workspace W"]

[[on-window-detected]]
if.app-id = "com.apple.Safari"
if.window-title-regex-substring = "dirtybiz"
run = ["layout tiling v_accordion", "move-node-to-workspace M"]

[[on-window-detected]]
if.app-name-regex-substring = "(firefox|google chrome)"
run = ["layout tiling vertical", "move-node-to-workspace W"]

[[on-window-detected]]
if.app-id = "com.apple.dt.Xcode"
run = "move-node-to-workspace --focus-follows-window 2"

# Media
[[on-window-detected]]
if.app-id = "com.brave.Browser"
run = ["layout tiling v_accordion", "move-node-to-workspace M"]

[[on-window-detected]]
if.app-id = "com.apple.TV"
run = ["layout tiling v_accordion", "move-node-to-workspace M"]

[[on-window-detected]]
if.app-id = "com.colliderli.iina"
run = "move-node-to-workspace M"

# Messaging
[[on-window-detected]]
if.app-id = "com.apple.MobileSMS"
run = ["layout tiling v_tiles", "move-node-to-workspace T"]

# Utilities
[[on-window-detected]]
if.app-id = "com.1password.1password"
run = "move-node-to-workspace --focus-follows-window 0"

[[on-window-detected]]
if.app-id = "com.apple.ActivityMonitor"
run = "move-node-to-workspace 0"

# Work Apps
#
[[on-window-detected]]
if.app-id = "com.jetbrains.intellij"
run = "move-node-to-workspace --focus-follows-window 2"

[[on-window-detected]]
if.app-id = "com.tinyspeck.slackmacgap"
run = "move-node-to-workspace T"
