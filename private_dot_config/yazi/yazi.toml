# A TOML linter such as https://taplo.tamasfe.dev/ can use this schema to validate your config.
# If you encounter any issues, please make an issue at https://github.com/yazi-rs/schemas.
#:schema /Users/moksha/.config/taplo/yazi.json

[mgr]
ratio = [1, 2, 2]

[opener]
play = [{ run = "mpv %s", orphan = true, for = "unix" }]

[plugin]
prepend_spotters = [{ mime = "video/*", run = "spot-video" }]
prepend_preloaders = [
  # Replace magick, image, video with mediainfo
  { mime = "{audio,video,image}/*", run = "mediainfo" },
  { mime = "application/subrip", run = "mediainfo" },
  # Adobe Illustrator, Adobe Photoshop is image/adobe.photoshop, already handled above
  { mime = "application/postscript", run = "mediainfo" },
]
# There are more extensions which are supported by mediainfo.
# Just add file's MIME type to `previewers`, `preloaders` above.
# https://mediaarea.net/en/MediaInfo/Support/Formats
prepend_previewers = [
  # Machine-specific: suppress previewing on external Seagate drive (slow HDD); harmless if drive absent
  { mime = "{audio,video,image}/*", run = "mediainfo" },
  { mime = "application/subrip", run = "mediainfo" },
  { mime = "application/postscript", run = "mediainfo" },
  { url = "**/*.plist", run = 'piper -- plutil -p "$1"' },
]

# We need custom for plist
# [[plugin.prepend_fetchers]]
# id = "mime"  # Remove if Yazi > v26.1.22
# url = "local://*"
# run = "mime-ext.local"
# prio = "high"
#
# [[plugin.prepend_fetchers]]
# id = "mime"  # Remove if Yazi > v26.1.22
# url = "remote://*"
# run = "mime-ext.remote"
# prio = "high"

# For a large file like Adobe Illustrator, Adobe Photoshop, etc
# you may need to increase the memory limit if no image is rendered.
# https://yazi-rs.github.io/docs/configuration/yazi#tasks
[tasks]
image_alloc = 1073741824  # = 1024*1024*1024 = 1024MB

[preview]
# Change them to your desired values
image_delay = 0
max_width = 1024
max_height = 768
